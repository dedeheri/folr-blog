import { logoDark, logoLight } from "../assets/image";

function NavbarAuth() {
  return (
    <div className="h-14 flex items-center">
      <img src={logoDark} className="w-24" />
    </div>
  );
}

export default NavbarAuth;
